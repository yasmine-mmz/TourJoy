{% extends 'BackOffice/navbars.html.twig' %}

{% block main %}
<div class="card-body">
<h5 class="card-title">Claims Table</h5>

<!-- Default Table -->
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
      <th scope="col">Date of creation</th>
      <th scope="col">State</th>
    <th scope="col">Category</th>
    </tr>
  </thead>
  <tbody>
    {% for Claims in Claimss %}
    <tr>
      <th scope="row">1</th>
      
    
<td>{{ Claims.title }}</td>
<td>{{ Claims.description }}</td>
<td>{{ Claims.createDate|date('Y-m-d') }}</td>
<td>{{ Claims.state }}</td>
<td>{{ Claims.fkC.name}}</td>
<td>

<a href ="{{path('Claims_update',{'id':Claims.id})}}">Update</a> |
<a href ="{{path('Claims_delete',{'id':Claims.id})}}">Delete</a> |

</td>
</tr>
{% endfor %}
</tbody>
</table>

<!-- End Default Table Example -->
</div>
{% endblock %}