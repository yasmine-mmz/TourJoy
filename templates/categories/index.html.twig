{% extends 'BackOffice/navbars.html.twig' %}

{% block main %}
<div class="card-body">
<h5 class="card-title">Categories Table</h5>

<!-- Default Table -->
<table class="table">
  <thead>
    <tr>
    <a href ="{{path('Categories_add')}}"class="btn btn-primary">Add category</a>
    
      <th scope="col">#</th>
      <th scope="col">Name</th>
    <th scope="col">Actions</th>

    </tr>
  </thead>
  <tbody>
    {% for Categories in Categoriess %}
    <tr>
    <th scope="row">{{ loop.index }}</th>

    
<td>{{ Categories.name }}</td>
  <td>
<a href ="{{path('Categories_update',{'id':Categories.id})}}"class="btn btn-primary">Update</a> |
<a href ="{{path('Categories_delete',{'id':Categories.id})}}"class="btn btn-danger">Delete</a> 

</td>
</tr>
{% endfor %}
</tbody>
</table>

<!-- End Default Table Example -->
</div>
{% endblock %}